package titocc.compiler;

/**
 * Enumeration of the registers in ttk-91 machine. Names are chosen so that .toString() method can
 * be used to output register names in the assembly code.
 */
public enum Register
{
	R0, R1, R2, R3, R4, R5, SP, FP
};
